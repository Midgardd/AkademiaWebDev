@model List<webdev.Models.Book>

<!DOCTYPE html>
<html>
<head>
    <style>
        .button1 {
            background-color: white;
            color: black;
            border: 2px solid #4CAF50;
        }
            .button1:hover {
                background-color: #4CAF50;
                color: white;
            }
    </style>
</head>
<body>
    <div>
        @using (Html.BeginForm("Create", "Book", FormMethod.Post))
        {
            <input type="text" name="Author" placeholder="Author" /> <br />
            <input type="text" name="Title" placeholder="Title" /> <br />
            <input type="text" name="Type" placeholder="BookType" /><br />
            <button class="button button1">Add</button>
        }
        </div>
        <div id="div1"></div>
        <div>
            <table>
                @foreach (var item in Model)
                {
                    <tr>


                        <td class="itemId" style="visibility:hidden">@item.Author</td>
                        <td><strong>Title:</strong></td>
                        <td>@item.Title</td>
                        <td><strong>Author:</strong></td>
                        <td class="author">@item.Author</td>
                        <td><strong>Type:</strong></td>
                        <td class="type">@item.Type</td>

                        <td>@Html.ActionLink("Delete1", "Delete", "Book", item)</td>
                        <td>@Html.ActionLink("Edit", "Edit", "Book", item)</td>

                        @if (item.Id == 1)
        {
                    <td> Bleble </td>

    }
    else
    {
                    <td> dupnyIndex</td>
    }

                    </tr>
    }
        </table>
    </div>
</body>
</html>